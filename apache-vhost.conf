<VirtualHost *:80>
    ServerName yourdomain.com
    ServerAlias www.yourdomain.com
    
    # Security Headers (removed X-Content-Type-Options to allow CSS)
    Header always set X-Frame-Options DENY
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    
    # Proxy settings
    ProxyPreserveHost On
    ProxyRequests Off
    
    # Proxy to Django application - let Django handle all requests including static files
    ProxyPass / http://127.0.0.1:8000/
    ProxyPassReverse / http://127.0.0.1:8000/
    
    # Logging
    ErrorLog ${APACHE_LOG_DIR}/ai-copywriter-error.log
    CustomLog ${APACHE_LOG_DIR}/ai-copywriter-access.log combined
</VirtualHost>